{
  "rules": {
    "users": {
      "$uid": {
        ".write": "$uid === auth.uid",
        ".create": "$uid === auth.uid",
        ".update": "$uid === auth.uid",
        ".delete": "$uid === auth.uid"
      }
    },
    ".read": true
  }
}