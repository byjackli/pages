import React from 'react';

class Loading extends React.PureComponent {
    render(){
        return(<main className="loading vrtCC">
            <p>loading...</p>
        </main>)
    }
}

export default Loading;